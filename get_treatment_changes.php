<?php
// require_once 'php/core/init.php';
// $override = new OverideData();
// $user = new User();
// header('Content-Type: application/json');

// // Get all treatment changes
// $treatment_changes = $override->getData('treatment_changes');
// echo json_encode($treatment_changes);


// // Include necessary PHP files and initialize objects
// $treatments = $override->getNewsAs2('treatment_changes', 'enrollment_id', $_GET['sid'], 'status', 1);
// foreach ($treatments as $treatment) {
//     // Generate HTML for each treatment (same as original table rows)
//     // Include update and delete modals within the loop
// }

?>
